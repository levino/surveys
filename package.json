{
  "name": "surveys-ui",
  "private": true,
  "description": "Tailwind+DaisyUI build for the surveys frontend (self-hosted, no CDN)",
  "scripts": {
    "build:css": "tailwindcss -i ./assets/input.css -o ./assets/app.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "daisyui": "^5.7.28",
    "tailwindcss": "^4.3.3"
  }
}
